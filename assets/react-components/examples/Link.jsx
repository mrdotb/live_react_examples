import { Link } from "live_react";

// Re-exported so the example page can show its source under the same name
// as every other example, and so the "React" tab has something to display.
export { Link };
